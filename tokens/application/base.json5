// These didn't fit in any other category, and
// https://code.visualstudio.com/api/references/theme-color
// calls these "base" colors
{
  // This applies to focused sections in the sidebar as well
  // as focused inputs
  "focusBorder": { "value": "{color.border.primary.50.value}" },
  "foreground": { "value": "{color.font.primary.50.value}" },
  "descriptionForeground": { "value": "{color.font.primary.40.value}" },
  "errorForeground": { "value": "{color.font.danger.value}" },
  
  // Icons are the ones in the panel, not the ones in the activity bar
  "icon": {
    "foreground": { "value": "{color.font.primary.50.value}" },
  },
  
  // Selection background for elements not in the editor, like selecting
  // text in an input
  "selection": {
    "background": { "value": "{color.background.selection.active.50.value}" }
  },
  
  // Widgets are the quick command palette, notifications, quick search, etc.
  "widget": {
    "shadow": { "value": "{color.background.primary.60.value}55" }
  }
}